export class AppSettings {
    public static PASS_PATTERN ='^(?=.*[0-9])(?=.*[a-zA-Z])([a-zA-Z0-9]+)$';
    public static SERVER_USER_URI = "http://localhost:8080/users";
	public static PASS_FIELD_ALERT = "Password should be min 4 char and must have digit and alphabet";
 }